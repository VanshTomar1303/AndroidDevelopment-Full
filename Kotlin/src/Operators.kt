fun main(){
    /*
Arithmatic Operator (+, -, *, %, /)
| Operator | Meaning             | Example |
| -------- | ------------------- | ------- |
| `+`      | Addition            | `a + b` |
| `-`      | Subtraction         | `a - b` |
| `*`      | Multiplication      | `a * b` |
| `/`      | Division            | `a / b` |
| `%`      | Modulus (remainder) | `a % b` |

Unary Operators
| Operator | Meaning                          | Example           |
| -------- | -------------------------------- | ----------------- |
| `+`      | Unary plus (returns value as is) | `+a`              |
| `-`      | Unary minus (negates value)      | `-a`              |
| `++`     | Increment                        | `a++` / `++a`     |
| `--`     | Decrement                        | `a--` / `--a`     |
| `!`      | Logical NOT                      | `!true` → `false` |

Assignment Operator
| Operator | Meaning                  | Example  |
| -------- | ------------------------ | -------- |
| `==`     | Equals                   | `a == b` |
| `!=`     | Not equals               | `a != b` |
| `<`      | Less than                | `a < b`  |
| `>`      | Greater than             | `a > b`  |
| `<=`     | Less than or equal to    | `a <= b` |
| `>=`     | Greater than or equal to | `a >= b` |

Comparison Operator
| Operator | Meaning                  | Example  |
| -------- | ------------------------ | -------- |
| `==`     | Equals                   | `a == b` |
| `!=`     | Not equals               | `a != b` |
| `<`      | Less than                | `a < b`  |
| `>`      | Greater than             | `a > b`  |
| `<=`     | Less than or equal to    | `a <= b` |
| `>=`     | Greater than or equal to | `a >= b` |

Logical Operator
| Operator | Meaning     | Example    |
| -------- | ----------- | ---------- |
| `&&`     | Logical AND | `a && b`   |
| `||`     | Logical OR    | `a || b` |
| `!`      | Logical NOT | `!a`       |

Range and Progression Operator
| Operator | Meaning                 | Example              |
| -------- | ----------------------- | -------------------- |
| `..`     | Range (inclusive)       | `1..5` → `1,2,3,4,5` |
| `..<`    | Range until (exclusive) | `1..<5` → `1,2,3,4`  |
| `downTo` | Decreasing range        | `5 downTo 1`         |
| `step`   | Step in a range         | `1..10 step 2`       |

Type Operator
| Operator | Meaning            | Example        |
| -------- | ------------------ | -------------- |
| `is`     | Type check         | `x is String`  |
| `!is`    | Not type check     | `x !is Int`    |
| `as`     | Type cast (unsafe) | `x as String`  |
| `as?`    | Safe cast          | `x as? String` |

Elvis operator
| Operator | Meaning                          | Example                         |
| -------- | -------------------------------- | ------------------------------- |
| `?:`     | Null-coalescing (Elvis operator) | `val name = input ?: "Default"` |


Safe call and Non null assertion
| Operator | Meaning            | Example      |
| -------- | ------------------ | ------------ |
| `?.`     | Safe call          | `obj?.name`  |
| `!!`     | Non-null assertion | `obj!!.name` |


Other Operator
| Operator | Meaning                                | Example                  |
| -------- | -------------------------------------- | ------------------------ |
| `in`     | Checks if value is in range/collection | `x in 1..10`             |
| `!in`    | Not in                                 | `x !in list`             |
| `::`     | Callable reference                     | `String::length`         |
| `?.let`  | Null-safe scope function               | `x?.let { println(it) }` |

     */

}